# Docker install instructions have you download the install package from the site
sudo apt install gnome-terminal
echo "[docker]: warn -- docker needs to be installed using custom docker instructions"
